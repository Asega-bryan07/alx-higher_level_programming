#!/usr/bin/node
/*
 * a function that returns the addition of 2 integers.
 *
 * The function must be visible from outside
 * The name of the function must be add
 * here we use exports instead of function
 */
exports.add = (a, b) => a + b;
